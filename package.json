{
  "name": "albato-apps-widget",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:global": "vite build --mode global",
    "build:all": "npm run build && npm run build:global",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "dompurify": "^3.0.9"
  }
}
